---
import { getExperiencesWithDynamicDuration, getExperienceStats } from '../data/experience'

// Get experiences with dynamic duration calculation
const experiences = getExperiencesWithDynamicDuration()
// Get dynamic statistics with calculated total years
const experienceStats = getExperienceStats()
---

<section id="experience" class="py-20 bg-gray-900/30">
  <div class="container mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        <span
          class="bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent"
        >
          Experiencia Profesional
        </span>
      </h2>
      <div
        class="w-24 h-1 bg-gradient-to-r from-blue-400 to-cyan-400 mx-auto mb-8"
      ></div>
      <p class="text-gray-300 text-lg max-w-3xl mx-auto">
        Mi trayectoria profesional en IT, desde soporte técnico hasta gestión de problemas corporativos,
        abarcando múltiples industrias y tecnologías.
      </p>
    </div>

    <!-- Experience Stats -->
    <div class="grid md:grid-cols-4 gap-6 mb-16">
      <div
        class="bg-gray-800/50 p-6 rounded-xl backdrop-blur-sm border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 text-center"
      >
        <div class="text-3xl font-bold text-blue-400 mb-2">{experienceStats.totalYears}+</div>
        <div class="text-gray-300">Años de Experiencia</div>
      </div>
      <div
        class="bg-gray-800/50 p-6 rounded-xl backdrop-blur-sm border border-gray-700/50 hover:border-cyan-500/50 transition-all duration-300 text-center"
      >
        <div class="text-3xl font-bold text-cyan-400 mb-2">{experienceStats.totalPositions}</div>
        <div class="text-gray-300">Posiciones</div>
      </div>
      <div
        class="bg-gray-800/50 p-6 rounded-xl backdrop-blur-sm border border-gray-700/50 hover:border-green-500/50 transition-all duration-300 text-center"
      >
        <div class="text-3xl font-bold text-green-400 mb-2">{experienceStats.companiesWorked}</div>
        <div class="text-gray-300">Empresas</div>
      </div>
      <div
        class="bg-gray-800/50 p-6 rounded-xl backdrop-blur-sm border border-gray-700/50 hover:border-purple-500/50 transition-all duration-300 text-center"
      >
        <div class="text-3xl font-bold text-purple-400 mb-2">{experienceStats.industriesCovered}</div>
        <div class="text-gray-300">Industrias</div>
      </div>
    </div>

    <!-- Experience Timeline -->
    <div class="relative">
      <!-- Timeline Line -->
      <div class="absolute left-8 top-0 bottom-0 w-0.5 bg-gradient-to-b from-blue-400 to-cyan-400 hidden md:block"></div>

      <!-- Experience Items -->
      <div class="space-y-12">
        {experiences.map((exp, index) => (
          <div class="relative">
            <!-- Timeline Dot -->
            <div class="absolute left-6 w-4 h-4 bg-gradient-to-r from-blue-400 to-cyan-400 rounded-full border-4 border-gray-900 hidden md:block"></div>
            
            <!-- Experience Card -->
            <div class="md:ml-20 bg-gray-800/50 rounded-2xl p-8 backdrop-blur-sm border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 hover:shadow-2xl hover:shadow-blue-500/10">
              <!-- Header -->
              <div class="flex flex-col lg:flex-row lg:items-start lg:justify-between mb-6">
                <div class="flex-1">
                  <h3 class="text-2xl font-bold text-white mb-2">{exp.title}</h3>
                  <div class="flex flex-col sm:flex-row sm:items-center gap-2 mb-3">
                    <span class="text-blue-400 font-semibold text-lg">{exp.company}</span>
                    <span class="text-gray-400">• {exp.companyType}</span>
                  </div>
                  <div class="flex flex-wrap gap-4 text-sm text-gray-300">
                    <span class="flex items-center gap-1">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a2 2 0 012-2h4a2 2 0 012 2v4m-6 0a2 2 0 00-2 2v1a2 2 0 002 2h8a2 2 0 002-2v-1a2 2 0 00-2-2m-6 0h8"></path>
                      </svg>
                      {exp.workType}
                    </span>
                    <span class="flex items-center gap-1">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                      </svg>
                      {exp.location}
                    </span>
                  </div>
                </div>
                <div class="text-right mt-4 lg:mt-0">
                  <div class="text-cyan-400 font-semibold mb-1">{exp.period}</div>
                  <div class="text-gray-400 text-sm">{exp.duration}</div>
                  {exp.linkedinHelped && (
                    <div class="mt-2 inline-flex items-center gap-1 text-xs text-green-400 bg-green-400/10 px-2 py-1 rounded-full">
                      <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                      </svg>
                      LinkedIn me ayudó a conseguir este trabajo
                    </div>
                  )}
                </div>
              </div>

              <!-- Description -->
              <p class="text-gray-300 mb-6 leading-relaxed">{exp.description}</p>

              <!-- Responsibilities -->
              <div class="mb-6">
                <h4 class="text-lg font-semibold text-white mb-3">Responsabilidades Principales:</h4>
                <ul class="space-y-2">
                  {exp.responsibilities.map((responsibility) => (
                    <li class="flex items-start gap-3 text-gray-300">
                      <svg class="w-5 h-5 text-blue-400 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                      </svg>
                      <span>{responsibility}</span>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- Technologies -->
              {exp.technologies && (
                <div class="mb-6">
                  <h4 class="text-lg font-semibold text-white mb-3">Tecnologías y Herramientas:</h4>
                  <div class="flex flex-wrap gap-2">
                    {exp.technologies.map((tech) => (
                      <span class="px-3 py-1 bg-blue-500/20 text-blue-300 rounded-full text-sm border border-blue-500/30">
                        {tech}
                      </span>
                    ))}
                  </div>
                </div>
              )}

              <!-- Achievements -->
              {exp.achievements && (
                <div>
                  <h4 class="text-lg font-semibold text-white mb-3">Logros Destacados:</h4>
                  <ul class="space-y-2">
                    {exp.achievements.map((achievement) => (
                      <li class="flex items-start gap-3 text-gray-300">
                        <svg class="w-5 h-5 text-yellow-400 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span>{achievement}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- Call to Action -->
    <div class="text-center mt-16">
      <div class="bg-gradient-to-r from-blue-500/10 to-cyan-500/10 rounded-2xl p-8 border border-blue-500/20">
        <h3 class="text-2xl font-bold text-white mb-4">¿Interesado en mi experiencia?</h3>
        <p class="text-gray-300 mb-6 max-w-2xl mx-auto">
          Con más de {experienceStats.totalYearsDisplay} años de experiencia en IT, estoy siempre abierto a nuevos desafíos y oportunidades de colaboración.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/cv_wdelcanto.pdf"
            target="_blank"
            class="inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-blue-500 to-cyan-500 hover:from-blue-600 hover:to-cyan-600 text-white font-semibold rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            Ver CV Completo
          </a>
          <a
            href="#contact"
            class="inline-flex items-center justify-center px-6 py-3 border-2 border-cyan-400 text-cyan-400 hover:bg-cyan-400 hover:text-white font-semibold rounded-lg transition-all duration-300 transform hover:scale-105"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
            </svg>
            Contactar
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
